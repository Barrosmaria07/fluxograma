using Microsoft.Maui.Controls;

namespace fluxogramadudi
{
    public partial class segundatelatransport : ContentPage
    {
        public segundatelatransport()
        {
            InitializeComponent();
        }

        private void OnRegisterButtonClicked(object sender, EventArgs e)
        {
            // Add navigation logic to the registration page here
        }

        // ... (You'll likely add search logic and update the resultLabel's text here)
    }
}
