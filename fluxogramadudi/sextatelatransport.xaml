<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="fluxogramadudi.sextatelatransport"
             Title="Dados da transportadora">

    <VerticalStackLayout Padding="20" Spacing="15">
        <Label Text="Dados da transportadora"
               FontSize="Title"
               HorizontalOptions="Center" />

        <Frame BackgroundColor="#F25CA2" 
               CornerRadius="10"
               HeightRequest="45"
               Padding="10, 0">
            <Entry Placeholder="Nome de usuário"
                   TextColor="White"
                   BackgroundColor="Transparent" />
        </Frame>

        <Frame BackgroundColor="#F25CA2"
               CornerRadius="10"
               HeightRequest="45"
               Padding="10, 0">
            <Entry Placeholder="E-mail"
                   Keyboard="Email"
                   TextColor="White"
                   BackgroundColor="Transparent" />
        </Frame>

        <Frame BackgroundColor="#F25CA2"
               CornerRadius="10"
               HeightRequest="45"
               Padding="10, 0">
            <Entry Placeholder="Telefone"
                   Keyboard="Telephone"
                   TextColor="White"
                   BackgroundColor="Transparent" />
        </Frame>
    </VerticalStackLayout>
</ContentPage>
